import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function AdminGiftsPage() {
  return (
    <PlaceholderPage
      title="Gift admin"
      description="List gift purchases and codes. Legacy: /admin/gift_admin"
      routeGroup="(admin)"
    />
  );
}
