import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function AdminReferralReportPage() {
  return (
    <PlaceholderPage
      title="Referral report"
      description="Payouts with referred users; approve/reject/restore. Legacy: /admin/referral_report"
      routeGroup="(admin)"
    />
  );
}
