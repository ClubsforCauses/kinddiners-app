import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function RestaurantLoginPage() {
  return (
    <PlaceholderPage
      title="Restaurant portal login"
      description="Login for restaurant partners. Legacy: /restaurant-login."
      routeGroup="(auth)"
    />
  );
}
