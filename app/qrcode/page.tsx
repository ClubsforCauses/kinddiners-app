import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function QrcodePage() {
  return (
    <PlaceholderPage
      title="QR code"
      description="User QR for redemption. Legacy: /qrcode"
      routeGroup="(member)"
    />
  );
}
