import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function AdminLoginPage() {
  return (
    <PlaceholderPage
      title="Admin login"
      description="Platform admin login. Legacy: /admin."
      routeGroup="(auth)"
    />
  );
}
