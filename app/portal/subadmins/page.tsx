import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function PortalSubadminsPage() {
  return (
    <PlaceholderPage
      title="Sub-admins"
      description="Add/remove sub-admins. Legacy: /restaurant-admin/add-subadmin"
      routeGroup="(portal)"
    />
  );
}
