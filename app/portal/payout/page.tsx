import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function PortalPayoutPage() {
  return (
    <PlaceholderPage
      title="Payout settings & report"
      description="Legacy: /set_payout, /payout_report"
      routeGroup="(portal)"
    />
  );
}
