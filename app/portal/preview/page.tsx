import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function PortalPreviewPage() {
  return (
    <PlaceholderPage
      title="Preview"
      description="Redirect to public restaurant page. Legacy: /restaurant-admin/preview/"
      routeGroup="(portal)"
    />
  );
}
