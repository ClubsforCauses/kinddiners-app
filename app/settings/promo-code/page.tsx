import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function PromoCodeSettingsPage() {
  return (
    <PlaceholderPage
      title="Ambassador / promo code settings"
      description="Legacy: /user/settings/promo-code"
      routeGroup="(member)"
    />
  );
}
