import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function VisitLogPage() {
  return (
    <PlaceholderPage
      title="Restaurant visit log"
      description="User's redemption log. Legacy: /restaurant-visit-log"
      routeGroup="(member)"
    />
  );
}
