import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function ManageMembershipPage() {
  return (
    <PlaceholderPage
      title="Manage membership"
      description="Legacy: /manage-membership"
      routeGroup="(member)"
    />
  );
}
