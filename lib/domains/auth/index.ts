/**
 * Auth domain
 * Login, logout, password reset for member, restaurant_admin, admin.
 * Source: REVERSE_ENGINEERED_SPEC_AND_REBUILD_PLAN.md
 */

export const AUTH_DOMAIN = "auth";

// Placeholder — implement with Supabase Auth
export function getAuthDomain() {
  return AUTH_DOMAIN;
}
