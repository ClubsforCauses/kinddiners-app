/**
 * Referrals domain
 * ReferralCode attribution, my_referrals, ambassador promo settings.
 * Models: referral_codes, profiles.referral_code.
 * Source: REVERSE_ENGINEERED_SPEC_AND_REBUILD_PLAN.md
 */

export const REFERRALS_DOMAIN = "referrals";

// Placeholder — implement attribution and lists
export function getReferralsDomain() {
  return REFERRALS_DOMAIN;
}
