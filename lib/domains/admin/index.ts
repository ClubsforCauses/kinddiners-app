/**
 * Admin domain
 * Restaurants CRUD, users/customers lists, content (FAQ, videos, ads, cuisines, ITB), gifts, payouts, logs, masquerade.
 * Source: REVERSE_ENGINEERED_SPEC_AND_REBUILD_PLAN.md
 */

export const ADMIN_DOMAIN = "admin";

// Placeholder — implement back-office and RLS
export function getAdminDomain() {
  return ADMIN_DOMAIN;
}
